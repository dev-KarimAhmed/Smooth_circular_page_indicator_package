name: smooth_circular_page_indicator
description: >
  A customizable smooth circular page indicator widget for Flutter.
  Perfect for onboarding, quizzes, or step-based UIs with built-in animations,
  icons, and tap handling.
version: 0.0.2
homepage: https://github.com/dev-KarimAhmed/Smooth_circular_page_indicator_package
repository: https://github.com/dev-KarimAhmed/Smooth_circular_page_indicator_package
issue_tracker: https://github.com/dev-KarimAhmed/Smooth_circular_page_indicator_package/issues

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

# You can add assets or fonts here if needed in the future.
